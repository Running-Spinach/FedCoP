# =============================================================================
# FedCoP — Python dependencies
# =============================================================================
# ✅ GPU 版 PyTorch(已直接配好 CUDA 12.8,下面 torch/torchvision 行会从
#    官方 GPU 索引安装):
#     pip install -r requirements.txt
#   验证:python -c "import torch; print(torch.cuda.is_available())"  → 应输出 True
#
# 若你的 CUDA 不是 12.8,把下面两行的 cu128 换成对应版本
# (cu118 / cu121 / cu124 等,见 https://pytorch.org/get-started/locally/)
#
# Core (required — all algorithms)
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.0
torchvision>=0.15
numpy>=1.21
tqdm>=4.64
pandas>=1.3
Pillow>=9.0
# 指标计算(AUROC/F1 等,论文主指标)需要 scikit-learn
scikit-learn>=1.0

# Optional (t-SNE 原型可视化绘图;save_protos_npy 存 .npy 不需要)
matplotlib>=3.5
