# =============================================================================
# D²-FL — Python dependencies
# =============================================================================
# Install: pip install -r requirements.txt
#
# Core (required — all algorithms)
torch>=2.0
torchvision>=0.15
numpy>=1.21
tqdm>=4.64
pandas>=1.3
Pillow>=9.0

# Optional (visualization only — t-SNE prototype plots)
scikit-learn>=1.0
matplotlib>=3.5
